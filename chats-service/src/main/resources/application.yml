server:
  port: 8089

spring:
  datasource:
    url: jdbc:sqlserver://${DB_HOST:localhost}:1433;databaseName=chatdb;trustServerCertificate=true
    username: sa
    password: yourStrong(!)Password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect
  application:
    name: CHATS-SERVICE
  config:
    import: optional:configserver:http://localhost:9296
